name: ci

on:
  push:
    branches:
      - main

jobs:

  tests:
    name: Style
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: check formatting
        run: npm run lint
